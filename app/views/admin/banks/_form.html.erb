<div class="form-group">
  <div class="container-fluid">
    <div class="row justify-content-center">
      <div class="col-4">
        <div class="card">
          <div class="card-body">
<%= form_for @bank, url: path, method: method_type, local: true do |f| %>

    <% if @bank.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@bank.errors.count, "error") %> prohibited
        this bank from being saved:
      </h2>
      <ul>
        <% @bank.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
      <div class="form-label"><%= f.label :title %></div>
      <%= f.text_field :title ,class: 'col-lg-4 form-control'%>
   <br/>
   <div class="btn btn-gray">
      <%= f.submit %>
      </div>
    
    
  <% end %>
</div>  
</div>
</div>
</div>
</div>
</div>
